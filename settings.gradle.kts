pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "KoltinMultiPlateformMobileFirstApp"
include(":firstKMPAndroidApp")
include(":shared")